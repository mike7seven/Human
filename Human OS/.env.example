# Human OS Cognitive API Configuration
# Copy this file to .env and modify as needed

# Server Configuration
PORT=8080
# ENV options: development, production
ENV=development

# Logging
# LOG_LEVEL options: debug, info, warn, error
LOG_LEVEL=info

# Database
# For local dev: ./humanOS.db
# For Docker: /app/data/humanOS.db (volume mounted)
DATABASE_PATH=./humanOS.db

# Docker Deployment Configuration
# For Docker deployment: Set this to your server's IP or domain
# The docker-start.sh script will set this automatically
# For local development: This is not needed (frontend/.env is used instead)
# VITE_API_URL=http://192.168.1.100:8080/api/v1
# VITE_API_URL=http://your-domain.com:8080/api/v1
